{
  "name": "express-demo-oneflow",
  "version": "1.3.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "semantic-release": "^24.0.0",
    "@semantic-release/commit-analyzer": "^13.0.0"
  },
  "release": {
    "branches": [
      "release/*",
      "hotfix/*"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      [
        "@semantic-release/npm",
        {
          "npmPublish": false
        }
      ]
    ]
  }
}
